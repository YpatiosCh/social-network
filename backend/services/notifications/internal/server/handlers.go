package server

// This is where all grpc endpoints live
// Need to be defined as rpc services to backend/shared/proto and add generation code into backend/shared/gen
// using protoc terminal commands. You add those in backend/shared/proto/protoMakefile and run at once from Makefile 'build-proto:'.
