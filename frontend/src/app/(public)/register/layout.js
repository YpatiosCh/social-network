import "../../globals.css";

export const metadata = {
  title: "SocialSphere - Register",
};

export default function RootLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}